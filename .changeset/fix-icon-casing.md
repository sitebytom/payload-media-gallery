---
"@sitebytom/payload-media-gallery": patch
---

Standardize icon casing to lowercase `src/icons` to resolve cross-platform build errors. Cleaned up redundant legacy component files from the `src` directory.
